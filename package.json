{
  "name": "nv-carvx-railway-bot",
  "version": "1.0.0",
  "description": "Nippon Vehicles CAR VX chassis lookup bot for Railway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.49.1",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
