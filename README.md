# NV CAR VX Render Bot v3 Safe Image

Strict image search version. Blocks bad marketplace/random images and only returns an image when the result text appears to match make/model/body code.

Deploy on Render as Docker.
